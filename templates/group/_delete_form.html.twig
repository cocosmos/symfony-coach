<form method="post" action="{{ path('app_group_delete', {'linkToken': group.linkToken}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ group.id) }}">
    <button class="btn">Delete</button>
</form>
